# Правила проєкту

## Git
- Усі зміни одразу пушити в `master` (і в робочу гілку сесії, якщо вона є). Перед пушем — піднімати `?v=…` у всіх файлах, щоб на телефоні не лишався старий кеш, і перезбирати `lessons/app.js` (`npm run build`), якщо змінювались `shared/path.js` чи `lessons/src`.
- Перед пушем: `npm run check` (юніт-тести, `tools/check-lessons.mjs`, `tools/check-pages.mjs`).

## Відео
- Будь-яке відео в застосунку показується ТІЛЬКИ через спільний плеєр `shared/vid.js`: квадратне, ▶/❚❚ по тапу, таймер «хв:сек / хв:сек», смужка перемотки, посилання «Дивитися на YouTube ↗» внизу під кнопкою «До вправ».
  - екран перед уроком: `LGVideo.overlay([id, заголовок, пояснення], onClose)`, або запис у таблиці `V` у `shared/vid.js` (ключ — кінець адреси сторінки);
  - відео всередині сторінки: `LGVideo.player(id)` → `{ el, destroy }`, `LGVideo.ytLink(id)`.
- Ніколи не вставляти голий `<iframe>` YouTube.
- Кожне нове відео, яке дає користувач, — одразу в цьому форматі.

## Пояснення перед уроками (замість «гайду»)
- Якщо в уроку немає відео, перед вправами показується «Пояснення». Воно має бути в кожному уроці.
- Формат (як у ChessKid «Capturing»): на весь екран; угорі іконка + назва уроку; дошка з анімацією; під дошкою панель ‹ ❚❚/▶ › (попередня / пауза / наступна анімація), стрілки ‹ › також по боках дошки; під нею текст, що змінюється разом з анімацією і прокручується вниз; внизу закріплена кнопка «Почати».
- 2–3 анімації на урок, кожна показує одну думку (напр. «Взяття»: три різні фігури по черзі когось б’ють).
- На дошці — мінімум фігур, лише ті, що потрібні для думки; короля можна не ставити.
- Анімація: хід фігури, підсвічені клітинки/стрілки, коротка пауза, повтор.
- Пояснення, у якому лише текст, — це баг: переробити на цей формат.
